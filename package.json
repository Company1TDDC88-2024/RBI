{
    "name": "fullstack-project",
    "private": true,
    "version": "1.0.0",
    "scripts": {
        "start": "concurrently \"cd Backend && poetry run flask --app server run --port=5555 --reload\" \"cd Frontend && npm start\""
    },
    "devDependencies": {
        "concurrently": "^9.0.1"
    }
}
