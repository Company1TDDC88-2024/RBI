import os
import subprocess

command = ["poetry", "run", "flask", "--app", "server", "run", "--port=5555"]

try:
    subprocess.run(command)
except KeyboardInterrupt:
    print("\nProcess interrupted. Exiting backend gracefully.")
