import subprocess

command = ["poetry", "run", "flask", "--app", "server", "run", "--port=5555", "--reload"]

try:
    subprocess.run(command)
except KeyboardInterrupt:
    print("\nExiting backend gracefully.")
